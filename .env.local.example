# ROI Frontend environment variables

# Phase 1 FastAPI backend base URL.
# Leave empty to run the frontend with mock data (useful for design review).
# Example: NEXT_PUBLIC_ROI_API_BASE=http://localhost:8000
NEXT_PUBLIC_ROI_API_BASE=
